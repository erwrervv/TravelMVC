@model Travel.Admin.Models.ArticleList

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>文章列表</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ArticleListId" />
            <div class="form-group">
                <label asp-for="ArticleListName" class="control-label"></label>
                <input asp-for="ArticleListName" class="form-control" />
                <span asp-validation-for="ArticleListName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MemberuniqueId" class="control-label"></label>
                <select asp-for="MemberuniqueId" class="form-control" asp-items="ViewBag.MemberuniqueId"></select>
                <span asp-validation-for="MemberuniqueId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
