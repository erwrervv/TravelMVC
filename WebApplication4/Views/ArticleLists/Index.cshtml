@model IEnumerable<Travel.Admin.Models.ArticleList>

@{
    ViewData["Title"] = "Index";
}

<h1>文章列表</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">新增</a>
</p>
<table class="table table-hover">
    <thead class="table-secondary">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ArticleListName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Memberunique.MemberName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ArticleListName)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Memberunique.MemberName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ArticleListId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ArticleListId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ArticleListId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
@section Styles {
    <link href="https://cdn.datatables.net/v/dt/dt-2.1.3/datatables.min.css" rel="stylesheet">
}

@section Scripts {
    <script src="https://cdn.datatables.net/v/dt/dt-2.1.3/datatables.min.js"></script>
    <script>
        $(document).ready(function () {
            //alert("ready");
            $("table").dataTable({
                // ajax: {
                //     type: "GET",
                //     url: "/ArticleOverviews/IndexJson",
                //     dataSrc: function (json) {
                //         return json;
                //     },
                // },

                columns: [
                    { "data": "ArticleListName", "width": "5%" },
                    { "data": "MemberName", "width": "5%" },
                    { "data": "", "width": "10%" },
                ],
                fixedHeader: { header: false },
                language: {
                    url: 'https://cdn.datatables.net/plug-ins/2.1.2/i18n/zh-HANT.json',
                },
            });
        });
    </script>

}